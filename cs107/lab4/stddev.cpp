#include <iostream>
using namespace std;

int main()
{
        // Definitions
        
        float s = 0.0;
        int n = 0;
        float x = 0;
        float t = 0;
        
        // Input/Process
        cout << "Let's compute standard deviation for a set of numbers." << 
                endl;
        while (n <= 0)                     //Get input 
        {
                cout << "Enter the number of data points you have: ";
                cin >> n;
        }
        for (int i = 1, i <= n, i++)       //Get data for points
        {
                //
        }
}
